import { ImpirialPipe } from './impirial.pipe';

describe('ImpirialPipe', () => {
    it('create an instance', () => {
        const pipe = new ImpirialPipe();
        expect(pipe).toBeTruthy();
    });
});
